\documentclass[sigconf,review,anonymous]{acmart}

\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

% RecSys 2026 submission mode (anonymous review).
% For camera-ready, switch documentclass to [sigconf] and replace rights metadata
% below with the exact values from ACM rights management.
\setcopyright{none}
\acmConference[RecSys '26]{Proceedings of the 20th ACM Conference on Recommender Systems}{September 28--October 2, 2026}{Minneapolis, MN, USA}
\acmBooktitle{Proceedings of the 20th ACM Conference on Recommender Systems (RecSys '26), September 28--October 2, 2026, Minneapolis, MN, USA}
\acmDOI{}
\acmISBN{}

\usepackage{array}
\usepackage{multirow}
\usepackage{tabularx}

\newcolumntype{Y}{>{\centering\arraybackslash}X}
\newcommand{\tblbest}[1]{\textbf{#1}}
\newcommand{\tblsecond}[1]{\underline{#1}}
\newcommand{\placeholderpanel}[1]{\fbox{\rule{0pt}{#1}\rule{0.92\linewidth}{0pt}}}

\begin{document}

\title[RouteRec for Sequential Recommendation]{RouteRec: Behavior-Guided Expert Routing for Sequential Recommendation}

\author{First Author}
\affiliation{%
  \institution{KAIST}
  \department{Kim Jaechul Graduate School of AI}
  \city{Daejeon}
  \country{Republic of Korea}}
\email{first.author@kaist.ac.kr}

\author{Second Author}
\affiliation{%
  \institution{KAIST}
  \department{Kim Jaechul Graduate School of AI}
  \city{Daejeon}
  \country{Republic of Korea}}
\email{second.author@kaist.ac.kr}

\renewcommand{\shortauthors}{Author et al.}

\begin{abstract}
Sequential recommendation is commonly optimized with one shared computation pipeline, even though interaction regimes vary substantially across users, sessions, and time scales. In practice, some prefixes are dominated by stable preference, some by repetition, and others by local exploration. This mismatch between behavioral diversity and shared computation is increasingly visible as backbones become stronger: performance improves, but the model still offers little control over \emph{how} different behavior patterns should trigger different computation.

Mixture-of-experts (MoE) is a natural candidate for conditional computation, yet many sequential MoE variants still route primarily from hidden states. This creates a semantic bottleneck: the same latent signal is asked to encode sequence content and decide routing allocation. As a result, expert assignment is often effective but opaque, making it difficult to interpret specialization, diagnose failure modes, or align routing with observable behavioral factors.

We propose \textbf{RouteRec}, a behavior-guided MoE framework that explicitly decouples routing signals from representation signals. RouteRec keeps a strong sequence encoder for hidden-state construction, while routing is driven by compact behavior cues extracted from standard logs (timestamps, optional categories, and interaction statistics). These cues are organized into four semantic families (Tempo, Focus, Memory, Exposure) and consumed through three temporal scopes (macro, mid, micro), so the routing structure matches evidence granularity rather than imposing a single gating bias everywhere.

This design is intentionally lightweight: we avoid heavy side-information modeling in the routing branch and instead emphasize interpretable, transferable summaries that are easy to compute across datasets. We further provide a dedicated preliminary formulation to make the method readable without overloading notation. Experiments show that RouteRec improves next-item recommendation over strong baselines, and diagnostic analyses indicate that improvements are associated with more behavior-consistent expert usage rather than unstructured parameter scaling.
\end{abstract}

\begin{CCSXML}
<ccs2012>
 <concept>
  <concept_id>10002951.10003317.10003347.10003350</concept_id>
  <concept_desc>Information systems~Recommender systems</concept_desc>
  <concept_significance>500</concept_significance>
 </concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Information systems~Recommender systems}

\keywords{sequential recommendation, mixture of experts, conditional computation, routing, behavioral features}

\maketitle

\section{Introduction}
Sequential recommendation is usually formulated as next-item prediction over an ordered interaction history. At first glance, this appears to be a homogeneous prediction task, but real user behavior is not homogeneous at all. Even for the same user, one session may reflect persistent preference, another may reflect short-term mission behavior, and another may be dominated by quick exploratory clicks. Prior surveys and studies consistently describe this multi-regime nature of sequential and session-based recommendation \cite{wang2019seqsurvey,wang2021sessionsurvey}.

Modern backbones have become very strong at sequence modeling \cite{hidasi2016gru4rec,kang2018sasrec,li2020tisasrec,qiu2022duorec,shin2024bsarec,liu2025sigma}, but they still predominantly rely on shared transformations across behaviorally different cases. This design is efficient and often performant on average, yet it leaves an important modeling question open: when two prefixes imply fundamentally different behavioral contexts, should they really invoke the same computation path?

Figure~\ref{fig:intro-behavioral-features} provides an intuitive view of this gap. Distinct users and sessions can exhibit markedly different tendencies in pace, repetition, and local exploration, but a one-path design does not expose a mechanism for behavior-specific computation allocation. In other words, representation quality improves, but routing semantics remain implicit.

\begin{figure}[!t]
  \centering
  \includegraphics[width=\columnwidth]{figures/figure1_different_user.pdf}
  \caption{Different users and sessions can require different computational paths.}
  \Description{A problem-motivation figure contrasting different user and session behavior patterns, highlighting that behaviorally different cases are often processed with the same computation path in standard sequential recommendation.}
  \label{fig:intro-behavioral-features}
\end{figure}

Mixture-of-experts (MoE) provides a direct mechanism for conditional computation \cite{jacobs1991adaptive,jordan1994hme,shazeer2017moe,fedus2022switch}. However, in many recommendation settings, routing is still derived from hidden states. This is practical, but it also entangles two roles that are better separated: representing sequence content and deciding where computation should go. When both are handled by the same latent signal, expert specialization becomes harder to interpret and debug.

This limitation becomes more serious when the goal is not only accuracy but also controllability and diagnosis. If routing is purely latent, it is difficult to answer basic questions such as: Which behavior pattern activated which expert? Are similar sessions routed consistently? Is an expert specializing in a meaningful regime or merely in an embedding artifact? These questions are central for robust deployment and scientific understanding, but are weakly supported by hidden-state-only routing.

We therefore adopt a simple principle: \textbf{decouple routing signals from representation signals}. In RouteRec, hidden states remain the content channel processed by experts, while routing is controlled by compact behavior cues traceable to logs. This separation preserves backbone strength while making routing decisions easier to inspect and reason about.

RouteRec also aligns routing with temporal scope. Behavior evidence near the prediction point is not equivalent to long-range history evidence; forcing a single routing bias for all horizons is unnecessarily rigid. We therefore use a multi-stage design with macro, mid, and micro routing, allowing coarse-to-fine expert allocation that reflects cross-session tendency, within-session evolution, and immediate transitions.

Our goal is not to maximize notation complexity or introduce heavy side branches. Instead, we emphasize a compact, transferable routing view built from broadly available signals. This makes the approach practical across datasets and improves method readability, while still enabling richer conditional computation than shared-path baselines.

This paper also introduces a dedicated \textit{Preliminary} section before the main method. The goal is pragmatic: define the problem statement, notation, and MoE basics once, so the method section can focus on RouteRec-specific design choices.

The main contributions of this work are as follows:
\begin{itemize}
  \item We identify a core limitation of hidden-state-only routing in sequential MoE recommendation and formalize routing-representation decoupling as a practical design principle.
  \item We introduce RouteRec, a behavior-guided multi-stage MoE framework that routes with compact cues from standard interaction logs instead of hidden-state-only gating.
  \item We provide a clear preliminary formulation (problem statement and MoE background) to support readable method development without excessive symbol overhead.
  \item We show that RouteRec improves recommendation quality over strong baselines and yields more interpretable, behavior-consistent routing patterns in analysis.
\end{itemize}

\section{Related Work}
\label{sec:related}

\subsection{Sequential Recommendation Backbones}
Sequential recommendation has progressed from recurrent and convolutional models to graph and Transformer architectures \cite{hidasi2016gru4rec,tang2018caser,wu2019srgnn,kang2018sasrec,sun2019bert4rec}. Recent methods further strengthen performance with time-aware attention, contrastive objectives, and structural inductive bias \cite{li2020tisasrec,xie2020cl4srec,wang2023multiplecontrast,qiu2022duorec,du2023fearec,shin2024bsarec,liu2025sigma}. These advances improve representation quality, but they generally retain shared computation across behaviorally different cases.

\subsection{Heterogeneous User State Modeling}
Many studies explicitly acknowledge non-stationary behavior, including short-term intent modeling and multi-interest extraction \cite{li2017narm,liu2018stamp,ying2018shan,ma2019hgn,li2019mind,cen2020comirec}. Most approaches handle heterogeneity inside representation learning, for example by attention reweighting or interest decomposition. RouteRec is complementary: it treats heterogeneity as a routing problem and uses explicit behavior cues to modulate computation paths.

\subsection{Feature-Aware Sequential Recommendation}
Feature-aware methods use timestamps, side information, and feature channels to strengthen sequence encoding \cite{li2020tisasrec,zhang2019fdsa,xie2022difsr,du2023fearec}. In most cases, these signals are fused into hidden representations. RouteRec uses a different role assignment: features are primarily routing controls, while representation transformation remains expert-specific over hidden states.

\subsection{MoE in Recommendation}
Classical and modern MoE work demonstrates the value of conditional computation \cite{jacobs1991adaptive,jordan1994hme,shazeer2017moe,fedus2022switch}. Recommendation-oriented expert architectures also show strong capacity benefits \cite{ma2018mmoe,tang2020ple,liu2025fame,bian2023m3srec,zhang2025hm4sr}. Our distinction is not simply adding experts, but making routing semantics behavior-grounded and interpretable through explicit cue design.

\section{Preliminary}
\label{sec:prelim}

\subsection{Problem Statement}
For each user, we observe interactions ordered by time and train the model for next-item prediction. In one training instance, we denote the observed prefix as $x_{1:t}$ and the target as $x_{t+1}$. Each interaction includes at least an item identity and timestamp, and may additionally include coarse category information depending on the dataset.

To keep the formulation readable, we use two signal channels throughout this paper. The first is the representation channel, where a sequential encoder maps observed prefixes to hidden states $\mathbf{h}_t$. The second is the behavior channel, where lightweight statistics from the same prefix are summarized into a compact cue vector $\mathbf{b}_t$. RouteRec uses $\mathbf{h}_t$ for expert transformation and $\mathbf{b}_t$ for routing decisions. This separation is the central modeling choice that the later method section instantiates stage by stage.

The behavior channel is intentionally simple: cues are derived from standard log attributes rather than from a deep side encoder. This keeps routing signals stable across datasets and easier to interpret during analysis. In practice, cues capture aspects such as pace, repetition tendency, focus concentration, and popularity exposure. We do not force a large handcrafted taxonomy in this section; instead, we keep the abstraction compact and defer stage-specific feature construction details to Method.

\subsection{MoE Background for Sequential Models}
In a standard MoE layer with $E$ experts, an expert-weighted output at position $t$ is
\begin{equation}
\tilde{\mathbf{h}}_t = \sum_{e=1}^{E} \pi_{t,e}\,\mathrm{Expert}_e(\mathbf{h}_t),
\label{eq:moe-basic}
\end{equation}
where $\boldsymbol{\pi}_t \in \mathbb{R}^{E}$ is a routing distribution with $\sum_e \pi_{t,e}=1$.

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/figure2_overall_arch_simple.pdf}
  \caption{Overall RouteRec framework. (a) RouteRec keeps a strong sequential backbone and replaces shared feed-forward blocks with behavior-guided MoE stages. (b) All stages use the same scalar-group conditional routing composition: a group-level distribution and a conditional intra-group distribution are multiplied to form expert probabilities. (c) Macro, mid, and micro differ by temporal evidence scope and routing granularity, not by router formula.}
  \Description{A three-panel architecture figure showing RouteRec with a unified scalar-group conditional router across macro, mid, and micro stages, where stages differ by temporal evidence scope and granularity.}
  \label{fig:framework}
\end{figure*}
In many practical systems, $\boldsymbol{\pi}_t$ is computed from hidden states. This is effective but often semantically opaque, because routing and content encoding are entangled in the same latent space. RouteRec follows a different view: routing is computed from behavior cues, while hidden states are dedicated to expert transformation. The distinction is conceptually small but operationally important for interpretability and diagnosis.

Another practical point is temporal scope. Sequential recommendation combines long-range tendency and short-range fluctuation, so a single routing granularity can be suboptimal. RouteRec addresses this by applying the same MoE interface at multiple stages with different evidence windows. This keeps notation light while allowing routing behavior to adapt across horizons.

In the final model used in our main experiments, this multi-stage principle is realized with a shared router composition across all stages, while stage differences are introduced through temporal evidence windows and routing granularity. The next section formalizes this design.

\section{Method}
\label{sec:method}

\subsection{RouteRec Overview}
\label{sec:problem-framework}

Figure~\ref{fig:framework} summarizes RouteRec. We retain a strong sequential backbone and replace shared feed-forward computation with stage-wise MoE routing. The core interface follows the preliminary setup: hidden states are transformed by experts, and routing is controlled by behavior cues.

Let $\mathcal{T}=\{\mathrm{macro},\mathrm{mid},\mathrm{micro}\}$ be the stage set. At stage $s \in \mathcal{T}$, routing uses a stage-specific behavior feature $\mathbf{f}^{(s)}_t$, while experts consume the shared hidden state $\mathbf{h}_t$:
\begin{align}
\boldsymbol{\pi}^{(s)}_t
&=
\mathrm{softmax}\!\left(\mathrm{Router}_s(\mathbf{f}^{(s)}_t)/\tau_s\right), \\
\tilde{\mathbf{h}}^{(s)}_t
&=
\sum_{e=1}^{E}\pi^{(s)}_{t,e}\,\mathrm{Expert}^{(s)}_e(\mathbf{h}_t).
\label{eq:stage-routing}
\end{align}
Eq.~\eqref{eq:stage-routing} is the common stage interface. In the final RouteRec variant, all stages instantiate this interface with the same routing composition, while temporal differences arise from stage windows and routing granularity.

\subsection{Behavioral Features from Raw Signals}
\label{sec:feature-construction}

RouteRec is designed for realistic data availability. It uses signals that are commonly present in interaction logs: item identity, timestamp, optional category, and optional corpus-level popularity. We intentionally avoid heavy side-information encoders in the routing branch. Instead, raw signals are converted into compact scalar summaries that are easy to compute, stable across datasets, and interpretable at analysis time.

For session $s_m$ and target position $t$, the stage windows are
\begin{align}
\mathcal{R}^{\mathrm{macro}}_t &= [s_{\max(1,m-H)},\dots,s_{m-1}], \\
\mathcal{R}^{\mathrm{mid}}_t &= [x_1,\dots,x_{t-1}], \\
\mathcal{R}^{\mathrm{micro}}_t &= [x_{\max(1,t-w)},\dots,x_{t-1}],
\end{align}
where $H=5$ and $w=5$ in our main setting. A shared summary operator $\mathcal{A}(\cdot)$ maps each window to a low-dimensional feature vector:
\[
\mathbf{f}^{(s)}_t=\mathcal{A}(\mathcal{R}^{(s)}_t),\qquad s\in\mathcal{T}.
\]

The summary dimensions are grouped into four semantic families: \textit{Tempo}, \textit{Focus}, \textit{Memory}, and \textit{Exposure}. Tempo captures pace and interval statistics. Focus captures concentration and switching behavior over coarse categories or item groups. Memory captures recurrence, repetition, and overlap with recent context. Exposure captures interactions with popularity strata and their local drift. This decomposition is semantic rather than architectural: it gives routing modules an interpretable behavioral basis.

Importantly, the feature template is shared across stages, but each stage reads it through a different temporal lens. Macro features reflect persistent tendency and broad preference shape. Mid features track evolving intent inside the current session. Micro features emphasize immediate cues around the prediction target. This shared-template, stage-specific-view design keeps the feature branch compact while preserving temporal specificity.

\subsection{Unified Router Composition}

The final RouteRec architecture uses one shared scalar-group conditional composition at every stage. Experts are partitioned into $G$ groups with $C$ experts per group, so $E=GC$. For stage $s$ and position $t$, the router produces
\begin{align}
p^{(s)}_{t,g} &= \mathrm{softmax}_g\!\left(z^{(s)}_{t,g}\right), \\
r^{(s)}_{t,g,c} &= \mathrm{softmax}_c\!\left(u^{(s)}_{t,g,c}\right),
\end{align}
where $p^{(s)}_{t,g}$ is a group-level routing distribution and $r^{(s)}_{t,g,c}$ is a conditional within-group routing distribution. The final expert probability is their product:
\begin{equation}
\pi^{(s)}_{t,g,c}=p^{(s)}_{t,g}\,r^{(s)}_{t,g,c},
\label{eq:group-conditional-product}
\end{equation}
followed by flattening $(g,c)$ to expert index $e$. Eq.~\eqref{eq:group-conditional-product} is the same for macro, mid, and micro.

This unified composition is important for two reasons. First, it gives all stages a shared structural prior, which reduces stage-specific router engineering and simplifies debugging. Second, it keeps diagnostics comparable across stages because differences in routing behavior can be attributed to evidence scope rather than formula changes.

In RouteRec, routing is driven by behavior cues, while hidden states remain dedicated to expert transformation in Eq.~\eqref{eq:stage-routing}. This preserves the routing-representation decoupling principle introduced in Preliminary.

\subsection{Stage Instantiation by Temporal Scope}

Although the router formula is shared, stages are not redundant. They differ in temporal evidence and routing granularity.

\paragraph{Macro and mid (session-level routing).}
Macro and mid compute one routing distribution per sequence from their respective windows, then broadcast it over valid positions inside the sequence. Macro uses broader cross-session history; mid uses within-session prefix evidence. This setting emphasizes stable allocation for slower-changing context.

\paragraph{Micro (token-level routing).}
Micro computes routing per token near the prediction target using short-horizon context. This enables fast adaptation to immediate transitions without changing the router form itself.

Formally, if $\Gamma_s$ denotes the granularity operator at stage $s$, then
\[
\begin{aligned}
\boldsymbol{\pi}^{(s)}_t &= \Gamma_s\!\left(\mathcal{R}^{(s)}_t\right), \\
\Gamma_{\mathrm{macro}},\Gamma_{\mathrm{mid}} &: \text{session-level}, \\
\Gamma_{\mathrm{micro}} &: \text{token-level}.
\end{aligned}
\]
Hence, RouteRec's coarse-to-fine behavior comes from stage evidence scope and update frequency, while the underlying router composition remains constant.

\subsection{Training Objective}
\label{sec:training-objective}

RouteRec is trained with a standard next-item objective and two router-side regularizers: route consistency and z-loss. The first regularizer shapes routing semantics, and the second stabilizes routing numerics.

Let $\mathbf{z}$ be final logits and $y$ be the target item. The main loss is
\[
\mathcal{L}_{\mathrm{CE}}
=
-\log \frac{\exp z_y}{\sum_{j}\exp z_j}.
\]
For route consistency, we first compute a sample-level routing distribution at each stage by averaging token-level routing weights:
\[
\bar{\boldsymbol{\pi}}_i^{\,s}
=
\frac{1}{L_i}\sum_{t=1}^{L_i}\boldsymbol{\pi}_{i,t}^{\,s}
\]
where $L_i$ is the valid sequence length. For each stage $s$, we construct a pair set $\mathcal{P}_s$ from feature-similar samples in the mini-batch and minimize
\[
\mathcal{L}_{\mathrm{cons}}
=
\frac{1}{|\mathcal{T}|}
\sum_{s \in \mathcal{T}}
\frac{1}{|\mathcal{P}_s|}
\sum_{(i,j)\in \mathcal{P}_s}
\mathrm{JSD}\!\left(\bar{\boldsymbol{\pi}}_i^{\,s}, \bar{\boldsymbol{\pi}}_j^{\,s}\right),
\]
which encourages neighboring behavioral states to produce compatible routing distributions. In other words, semantically similar inputs should not induce arbitrarily different routing behavior.

\begin{table*}[t]
  \centering
  \small
  \setlength{\tabcolsep}{3.4pt}
  \renewcommand{\arraystretch}{1.02}
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{ll*{11}{r}}
    \toprule
    Dataset & Metric & GRU4Rec & SASRec & TiSASRec & DuoRec & FEARec & BSARec & FAME & DIF-SR & FDSA & RouteRec & Improv. \\
    \midrule
    \multirow{3}{*}{Beauty}
      & HR@10   & 0.0946 & 0.1288 & 0.1318 & 0.1318 & \tblsecond{0.1461} & 0.0774 & 0.0458 & 0.1203 & 0.1347 & \tblbest{0.1616} & +10.6\% \\
      & NDCG@10 & 0.0520 & 0.0851 & 0.0912 & 0.0802 & 0.0858 & 0.0567 & 0.0339 & 0.0844 & \tblsecond{0.0943} & \tblbest{0.0982} & +4.1\% \\
      & MRR@20  & 0.0411 & 0.0735 & \tblsecond{0.0839} & 0.0686 & 0.0692 & 0.0519 & 0.0313 & 0.0752 & \tblbest{0.0841} & 0.0822 & -2.3\% \\
    \midrule
    \multirow{3}{*}{Foursquare}
      & HR@10   & 0.2218 & \tblsecond{0.3197} & 0.3106 & 0.3122 & 0.3070 & 0.2341 & 0.2046 & 0.2575 & 0.2998 & \tblbest{0.3225} & +0.9\% \\
      & NDCG@10 & 0.1497 & 0.2033 & \tblsecond{0.2037} & 0.1950 & 0.1935 & 0.1594 & 0.1396 & 0.1784 & 0.2001 & \tblbest{0.2041} & +0.2\% \\
      & MRR@20  & 0.1297 & 0.1719 & \tblbest{0.1742} & 0.1623 & 0.1624 & 0.1385 & 0.1214 & 0.1563 & \tblsecond{0.1723} & 0.1712 & -1.7\% \\
    \midrule
    \multirow{3}{*}{KuaiRec}
      & HR@10   & 0.3024 & 0.3427 & 0.3427 & 0.3415 & 0.3438 & 0.3359 & 0.3315 & 0.3113 & \tblsecond{0.3471} & \tblbest{0.3617} & +4.2\% \\
      & NDCG@10 & 0.2585 & \tblsecond{0.3366} & 0.3076 & 0.3203 & 0.3235 & 0.3314 & 0.3300 & 0.2953 & 0.3239 & \tblbest{0.3454} & +2.6\% \\
      & MRR@20  & 0.2470 & \tblsecond{0.3350} & 0.2979 & 0.3155 & 0.3184 & 0.3300 & 0.3300 & 0.2906 & 0.3182 & \tblbest{0.3430} & +2.4\% \\
    \midrule
    \multirow{3}{*}{LastFM}
      & HR@10   & 0.3090 & \tblsecond{0.3882} & 0.3794 & 0.3831 & 0.3845 & 0.3467 & 0.3355 & 0.3586 & 0.3657 & \tblbest{0.3893} & +0.3\% \\
      & NDCG@10 & 0.2673 & \tblsecond{0.3260} & 0.3211 & 0.3083 & 0.3127 & 0.3053 & 0.3012 & 0.3195 & 0.3171 & \tblbest{0.3297} & +1.1\% \\
      & MRR@20  & 0.2561 & 0.3075 & 0.3045 & 0.2863 & 0.2911 & 0.2930 & 0.2914 & \tblsecond{0.3083} & 0.3033 & \tblbest{0.3121} & +1.2\% \\
    \midrule
    \multirow{3}{*}{ML-1M}
      & HR@10   & 0.1408 & 0.1478 & 0.1538 & 0.1399 & 0.1422 & \tblsecond{0.1589} & 0.1557 & 0.1501 & \tblbest{0.1594} & 0.1515 & -5.0\% \\
      & NDCG@10 & 0.0745 & 0.0770 & 0.0844 & 0.0728 & 0.0731 & \tblbest{0.0854} & \tblsecond{0.0853} & 0.0764 & 0.0835 & 0.0769 & -10.0\% \\
      & MRR@20  & 0.0591 & 0.0608 & \tblbest{0.0690} & 0.0576 & 0.0577 & 0.0678 & \tblsecond{0.0687} & 0.0595 & 0.0658 & 0.0594 & -13.9\% \\
    \midrule
    \multirow{3}{*}{Retail Rocket}
      & HR@10   & 0.5211 & \tblsecond{0.5605} & 0.5435 & 0.5377 & 0.5441 & 0.4658 & 0.4507 & 0.4365 & 0.5133 & \tblbest{0.5612} & +0.1\% \\
      & NDCG@10 & 0.3858 & \tblsecond{0.4116} & 0.3963 & 0.3864 & 0.3935 & 0.3994 & 0.3935 & 0.3900 & 0.3651 & \tblbest{0.4150} & +0.8\% \\
      & MRR@20  & 0.3479 & 0.3696 & 0.3547 & 0.3441 & 0.3515 & \tblbest{0.3789} & \tblsecond{0.3753} & 0.3744 & 0.3239 & 0.3737 & -1.4\% \\
    \bottomrule
  \end{tabular}}
  \caption{Seen-target test results in the main text. We report HR@10, NDCG@10, and MRR@20 here, and defer the full $k\in\{5,10,20\}$ grid to Appendix~A. Bold and underlined values denote the best and second-best results.}
  \label{tab:main-overall}
\end{table*}
To stabilize gating numerics, we adopt z-loss on router logits~\cite{zoph2022stmoe}:
\[
\mathcal{L}_{z}
=
\frac{1}{|\mathcal{T}|}
\sum_{s \in \mathcal{T}}
\frac{1}{N_s}
\sum_{(i,t)\in\mathcal{V}_s}
\left(
\log \sum_{e=1}^{E}\exp q_{i,t,e}^{\,s}
\right)^2,
\]
where $\mathcal{V}_s$ is the set of valid positions and $N_s=|\mathcal{V}_s|$. This term discourages excessive logit scale and helps prevent unstable, overly sharp routing. The final objective is
\[
\mathcal{L}
=
\mathcal{L}_{\mathrm{CE}}
+
\lambda_{\mathrm{cons}}\,\mathcal{L}_{\mathrm{cons}}
+
\lambda_{z}\,\mathcal{L}_{z},
\]
where $\lambda_{\mathrm{cons}}$ and $\lambda_z$ are trade-off coefficients. This objective jointly optimizes prediction quality, semantic consistency of routing, and numerical stability of expert selection.

\section{Experiments}
\label{sec:experiments}


\subsection{Experimental Setup}
We evaluate RouteRec on six public datasets. These datasets cover e-commerce, location recommendation, short-video consumption, music listening, movie preference, and anonymous clickstream behavior. The six corpora are Beauty, Foursquare, KuaiRec, LastFM, ML-1M, and Retail Rocket. Appendix~A summarizes their provenance and processed statistics.

Across all datasets, interactions are first ordered chronologically and converted into session sequences. We then iteratively remove sessions shorter than $5$ and items with frequency below $3$, and split sessions into train/validation/test partitions in chronological order. The evaluation pipeline also cleans the held-out tail by re-forming contiguous validation and test segments and removing non-target unseen rows while preserving unseen targets, so that the final test setting remains strict but realistic. Beauty is derived from Amazon Beauty review logs and removes interactions with rating below $3$. For KuaiRec and LastFM, we use compact filtered subsets to keep the experimental budget practical while preserving the underlying behavioral structure. When category or time signals are incomplete, RouteRec falls back to the same portable Tempo, Focus, Memory, and Exposure feature families computed from the available interaction log.

The baselines follow three groups. The first group contains conventional sequential recommenders, namely GRU4Rec, SASRec, and TiSASRec~\cite{hidasi2016gru4rec,kang2018sasrec,li2020tisasrec}. The second group contains recent strong backbones, including DuoRec, FEARec, and BSARec~\cite{qiu2022duorec,du2023fearec,shin2024bsarec}. The third group contains feature-aware or expert-related comparators, including FAME, DIF-SR, and FDSA~\cite{liu2025fame,xie2022difsr,zhang2019fdsa}. All methods are trained under the same optimization family with matched early stopping and comparable capacity ranges. Table~\ref{tab:main-overall} reports the compact seen-target test summary with HR@10, NDCG@10, and MRR@20, while Appendix~A restores the full $3\times3$ cutoff grid under the same evaluation view. We use MRR@20 as the default scalar summary when a single validation or analysis metric is needed.

\subsection{Overall Performance: Does Behavior-Guided Routing Help?}
\label{sec:overall-performance}
Table~\ref{tab:main-overall} presents the main seen-target comparison in the text. The selected metrics balance retrieval quality, rank sensitivity, and early precision, so the reader can assess the central result without scanning the full cutoff grid. RouteRec achieves the strongest pattern on Beauty, KuaiRec, LastFM, and Retail Rocket, remains competitive on Foursquare, and trails the strongest baselines on ML-1M, where shared-backbone models are already highly optimized.

Two points are worth emphasizing. First, the clearest gains appear on Beauty, KuaiRec, and Retail Rocket, showing that behavior-guided routing helps both sparse revisit settings and highly drift-prone short-horizon settings. Second, the gain does not follow a simple capacity story: several baselines already use richer encoders or auxiliary objectives, yet the advantage still emerges when computation is routed according to behavior rather than shared uniformly. The detailed @5/@10/@20 table is moved to Appendix~A so that the main paper retains a readable experiments flow while still exposing the full result grid.

\subsection{What Should Control Routing?}
\label{sec:routing-control}
Figure~\ref{fig:routing-control-panels} compares four routing controls: a shared FFN, hidden-only routing, mixed hidden+behavior routing, and the final behavior-guided router. Hidden-only routing can improve over a shared FFN, but the gain is smaller and less stable than behavior-guided routing. Re-injecting hidden states into the router also fails to produce a consistent advantage over the behavior-only design.

The consistency panel makes the structural reason clearer. When routing is tied to observable cues rather than hidden-state geometry alone, behaviorally similar cases are assigned more compatible expert patterns. The advantage of RouteRec therefore comes from grounding the routing decision in semantically readable evidence, not from adding a more complicated gate.

\begin{figure}[!t]
  \centering
  \begin{minipage}[t]{0.48\columnwidth}
    \centering
    \placeholderpanel{1.25in}

    \vspace{0.35em}
    {\footnotesize (a) Avg. ranking quality across routing controls}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.48\columnwidth}
    \centering
    \placeholderpanel{1.25in}

    \vspace{0.35em}
    {\footnotesize (b) Route consistency over feature-similarity buckets}
  \end{minipage}
  \caption{Routing-control comparison. (a) Average MRR@20 across shared FFN, hidden-only, mixed, and behavior-guided routing. (b) Routing similarity over feature-similarity buckets.}
  \Description{A two-panel placeholder figure comparing routing-control variants by average ranking quality and routing consistency over feature-similarity buckets.}
  \label{fig:routing-control-panels}
\end{figure}

\subsection{Why Is the Staged RouteRec Structure Effective?}
\label{sec:stage-structure}
Figure~\ref{fig:stage-structure-panels} examines the RouteRec architecture itself. Panel (a) removes macro, mid, or micro routing to isolate the role of each temporal scope. Panel (b) separates staged routing from generic FFN replacement by comparing dense replacement against one-, two-, and three-stage layouts. Panel (c) tests wrapper-map and stage-order variants to show that the final composition is not an arbitrary design choice.

Across all three views, the same pattern is expected. Performance rises when routing is distributed across temporal scopes, dense replacement alone does not recover the same gain, and the final wrapper map remains among the strongest choices. The benefit of RouteRec therefore comes from coarse-to-fine behavioral allocation rather than from expert insertion by itself.

\begin{figure*}[!t]
  \centering
  \begin{minipage}[t]{0.31\textwidth}
    \centering
    \placeholderpanel{1.05in}

    \vspace{0.35em}
    {\footnotesize (a) Stage removal}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.31\textwidth}
    \centering
    \placeholderpanel{1.05in}

    \vspace{0.35em}
    {\footnotesize (b) Dense replacement vs. staged routing}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.31\textwidth}
    \centering
    \placeholderpanel{1.05in}

    \vspace{0.35em}
    {\footnotesize (c) Wrapper-map and order variants}
  \end{minipage}
  \caption{Stage and layout ablations. (a) Stage removal. (b) Dense replacement versus staged routing. (c) Wrapper-map and stage-order variants.}
  \Description{A three-panel placeholder figure for stage removal, dense-versus-staged routing comparisons, and wrapper-map or stage-order variants.}
  \label{fig:stage-structure-panels}
\end{figure*}

\subsection{Are Lightweight Cues Enough?}
\label{sec:lightweight-cues}
Figure~\ref{fig:cue-ablation-panels} evaluates whether RouteRec depends on rich metadata or whether ordinary interaction logs already provide enough routing evidence. The left panel removes cue families from the full model, and the right panel normalizes each variant by the gain of the full-cue model. Removing category-derived or time-derived cues lowers performance, but the sequence-only variant still retains a substantial fraction of the improvement over the shared-backbone baseline.

This result strengthens the portability claim. RouteRec benefits from richer evidence when it is available, but it does not require a heavy side-information stack. A meaningful part of the routing gain can already be recovered from the signals that most sequential recommendation systems already log.

\begin{figure}[!t]
  \centering
  \begin{minipage}[t]{0.48\columnwidth}
    \centering
    \placeholderpanel{1.25in}

    \vspace{0.35em}
    {\footnotesize (a) Avg. MRR@20 by cue family removal}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.48\columnwidth}
    \centering
    \placeholderpanel{1.25in}

    \vspace{0.35em}
    {\footnotesize (b) Relative retention of the full-cue gain}
  \end{minipage}
  \caption{Cue-family ablations. (a) Average MRR@20 under cue removal. (b) Gain retention relative to the full cue set.}
  \Description{A two-panel placeholder figure for cue-family ablations and normalized gain retention.}
  \label{fig:cue-ablation-panels}
\end{figure}

Detailed full-cutoff results and extended analyses, including slice-wise and transfer studies, are reported in Appendices~A--F.

\section{Conclusion}
\label{sec:conclusion}
RouteRec starts from a simple hypothesis: when behavioral evidence changes, the computation path should change as well. The experiments support this view from three main angles. First, RouteRec improves next-item ranking over strong sequential baselines across most datasets and cutoffs. Second, the gain comes from the routing signal and the staged design rather than from inserting experts indiscriminately. Third, lightweight cues derived from ordinary interaction logs already recover a substantial share of the benefit, which keeps the method practical even when side information is limited.

Taken together, these results suggest that compact behavioral cues provide a useful control interface for conditional computation in recommendation. Future work can build on this interface with broader transfer regimes, richer but still robust cue construction, and tighter deployment-aware constraints such as latency, continual adaptation, and router calibration.

\bibliographystyle{ACM-Reference-Format}
\bibliography{refs}

\nobalance
\clearpage
\appendix

\section{Dataset, Evaluation, and Full Results}
\label{app:data-details}
This appendix summarizes the six datasets and the common evaluation protocol. The original releases are cited here for completeness~\cite{mcauley2015amazon,yang2010foursquare,gao2022kuairec,celma2010music,harper2015movielens,retailrocket2015dataset}. The dataset descriptions emphasize domain differences and signal availability because the routing branch depends on which behavioral evidence is exposed in each corpus.

\subsection{Datasets and Protocol}

\begin{table}[t]
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3.0pt}
  \begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}}llrrrc@{}}
    \toprule
    Dataset & Domain & Interactions & Sessions & Items & Avg. len. \\
    \midrule
    Beauty & E-com. & 33,488 & 4,243 & 3,625 & 7.9 \\
    Foursquare & POI & 145,238 & 25,369 & 30,588 & 5.7 \\
    KuaiRec & Video & 287,411 & 24,458 & 6,477 & 11.8 \\
    LastFM & Music & 470,408 & 25,089 & 52,510 & 18.8 \\
    ML-1M & Movie & 575,281 & 14,539 & 3,533 & 39.6 \\
    Retail Rocket & Browse & 821,243 & 153,092 & 90,211 & 5.4 \\
    \bottomrule
  \end{tabular*}
  \caption{Datasets used in the experiments.}
  \label{tab:appendix-datasets}
\end{table}

Beauty and Retail Rocket stress sparse e-commerce revisit behavior, Foursquare and KuaiRec emphasize stronger short-horizon intent changes, and LastFM and ML-1M provide longer preference-driven histories. This spread is useful because it tests whether behavior-guided routing helps only in highly dynamic settings or remains competitive when preference structure is more stable.

Signal availability also differs in a way that matters for routing. Beauty exposes item, time, and product-category cues after low-rating removal. Foursquare includes item, timestamp, and venue-category structure with bursty check-in dynamics. KuaiRec offers item, time, and coarse content-side metadata in short-video sessions with strong short-term intent shifts. LastFM provides repeated listening histories with artist-side signals, while ML-1M contributes longer preference-driven sequences with genre information. Retail Rocket is the sparsest side-information setting, relying mainly on anonymous event streams with item and time signals.

All datasets are sessionized from timestamp-ordered interactions. We iteratively remove sessions shorter than $5$ and items with fewer than $3$ interactions until convergence, then split sessions chronologically into training, validation, and test partitions. The held-out tails are re-formed into contiguous validation and test segments, and non-target unseen rows are removed while unseen targets are preserved. We report HR, NDCG, and MRR at $k\in\{5,10,20\}$ for all methods, and use validation MRR@20 for model selection when a single scalar criterion is required.

\subsection{Full Ranking Results}
Table~\ref{tab:appendix-full-overall} restores the full seen-target cutoff grid that is summarized more compactly in Table~\ref{tab:main-overall}. This presentation is useful for checking whether the RouteRec advantage is confined to one evaluation cutoff or remains visible across shallower and deeper retrieval depth.

\begin{table*}[t]
  \centering
  \scriptsize
  \setlength{\tabcolsep}{2.8pt}
  \renewcommand{\arraystretch}{0.96}
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{ll*{11}{r}}
    \toprule
    Dataset & Metric & GRU4Rec & SASRec & TiSASRec & DuoRec & FEARec & BSARec & FAME & DIF-SR & FDSA & RouteRec & Improv. \\
    \midrule
    \multirow{9}{*}{Beauty}
      & HR@5    & 0.0602 & \tblbest{0.1101} & 0.0917 & 0.0946 & \tblsecond{0.1060} & 0.0716 & 0.0372 & 0.0974 & \tblsecond{0.1060} & 0.1054 & -4.3\% \\
      & HR@10   & 0.0946 & 0.1288 & 0.1318 & 0.1318 & \tblsecond{0.1461} & 0.0774 & 0.0458 & 0.1203 & 0.1347 & \tblbest{0.1616} & +10.6\% \\
      & HR@20   & 0.1232 & 0.1616 & \tblsecond{0.1977} & 0.1920 & 0.1748 & 0.1032 & 0.0602 & 0.1461 & 0.1633 & \tblbest{0.2014} & +1.9\% \\
      & NDCG@5  & 0.0412 & 0.0786 & 0.0785 & 0.0684 & 0.0732 & 0.0550 & 0.0312 & 0.0771 & \tblbest{0.0852} & \tblsecond{0.0803} & -5.8\% \\
      & NDCG@10 & 0.0520 & 0.0851 & 0.0912 & 0.0802 & 0.0858 & 0.0567 & 0.0339 & 0.0844 & \tblsecond{0.0943} & \tblbest{0.0982} & +4.1\% \\
      & NDCG@20 & 0.0589 & 0.0933 & \tblsecond{0.1081} & 0.0955 & 0.0930 & 0.0634 & 0.0375 & 0.0911 & 0.1017 & \tblbest{0.1083} & +0.2\% \\
      & MRR@5   & 0.0350 & 0.0684 & \tblsecond{0.0741} & 0.0596 & 0.0622 & 0.0494 & 0.0292 & 0.0703 & \tblbest{0.0784} & 0.0721 & -8.0\% \\
      & MRR@10  & 0.0393 & 0.0712 & 0.0792 & 0.0643 & 0.0672 & 0.0500 & 0.0303 & 0.0733 & \tblbest{0.0820} & \tblsecond{0.0794} & -3.2\% \\
      & MRR@20  & 0.0411 & 0.0735 & \tblsecond{0.0839} & 0.0686 & 0.0692 & 0.0519 & 0.0313 & 0.0752 & \tblbest{0.0841} & 0.0822 & -2.3\% \\
    \midrule
    \multirow{9}{*}{Foursquare}
      & HR@5    & 0.1771 & 0.2446 & \tblsecond{0.2558} & 0.2378 & 0.2398 & 0.1996 & 0.1731 & 0.2152 & 0.2478 & \tblbest{0.2562} & +0.2\% \\
      & HR@10   & 0.2218 & \tblsecond{0.3197} & 0.3106 & 0.3122 & 0.3070 & 0.2341 & 0.2046 & 0.2575 & 0.2998 & \tblbest{0.3225} & +0.9\% \\
      & HR@20   & 0.2566 & \tblbest{0.3837} & 0.3669 & 0.3605 & 0.3653 & 0.2742 & 0.2370 & 0.2962 & 0.3477 & \tblsecond{0.3777} & -1.6\% \\
      & NDCG@5  & 0.1352 & 0.1791 & \tblbest{0.1858} & 0.1708 & 0.1719 & 0.1480 & 0.1293 & 0.1645 & \tblsecond{0.1834} & 0.1827 & -1.7\% \\
      & NDCG@10 & 0.1497 & 0.2033 & \tblsecond{0.2037} & 0.1950 & 0.1935 & 0.1594 & 0.1396 & 0.1784 & 0.2001 & \tblbest{0.2041} & +0.2\% \\
      & NDCG@20 & 0.1585 & \tblbest{0.2195} & 0.2180 & 0.2075 & 0.2083 & 0.1695 & 0.1478 & 0.1882 & 0.2123 & \tblsecond{0.2181} & -0.6\% \\
      & MRR@5   & 0.1213 & 0.1575 & \tblbest{0.1628} & 0.1487 & 0.1494 & 0.1309 & 0.1148 & 0.1478 & \tblsecond{0.1620} & 0.1584 & -2.7\% \\
      & MRR@10  & 0.1273 & 0.1675 & \tblbest{0.1703} & 0.1588 & 0.1583 & 0.1357 & 0.1192 & 0.1535 & \tblsecond{0.1689} & 0.1673 & -1.8\% \\
      & MRR@20  & 0.1297 & 0.1719 & \tblbest{0.1742} & 0.1623 & 0.1624 & 0.1385 & 0.1214 & 0.1563 & \tblsecond{0.1723} & 0.1712 & -1.7\% \\
    \midrule
    \multirow{9}{*}{KuaiRec}
      & HR@5    & 0.2710 & 0.3359 & 0.3225 & 0.3259 & \tblsecond{0.3382} & 0.3337 & 0.3303 & 0.3046 & 0.3281 & \tblbest{0.3471} & +2.6\% \\
      & HR@10   & 0.3024 & 0.3427 & 0.3427 & 0.3415 & 0.3438 & 0.3359 & 0.3315 & 0.3113 & \tblsecond{0.3471} & \tblbest{0.3617} & +4.2\% \\
      & HR@20   & 0.3337 & 0.3460 & 0.3628 & \tblsecond{0.3684} & 0.3639 & 0.3371 & 0.3359 & 0.3169 & 0.3673 & \tblbest{0.3975} & +7.9\% \\
      & NDCG@5  & 0.2480 & \tblsecond{0.3344} & 0.3009 & 0.3151 & 0.3218 & 0.3307 & 0.3297 & 0.2930 & 0.3179 & \tblbest{0.3407} & +1.9\% \\
      & NDCG@10 & 0.2585 & \tblsecond{0.3366} & 0.3076 & 0.3203 & 0.3235 & 0.3314 & 0.3300 & 0.2953 & 0.3239 & \tblbest{0.3454} & +2.6\% \\
      & NDCG@20 & 0.2664 & \tblsecond{0.3374} & 0.3126 & 0.3271 & 0.3286 & 0.3317 & 0.3312 & 0.2967 & 0.3289 & \tblbest{0.3545} & +5.1\% \\
      & MRR@5   & 0.2403 & \tblsecond{0.3339} & 0.2937 & 0.3115 & 0.3163 & 0.3296 & 0.3295 & 0.2892 & 0.3145 & \tblbest{0.3386} & +1.4\% \\
      & MRR@10  & 0.2448 & \tblsecond{0.3348} & 0.2966 & 0.3136 & 0.3170 & 0.3299 & 0.3296 & 0.2902 & 0.3168 & \tblbest{0.3405} & +1.7\% \\
      & MRR@20  & 0.2470 & \tblsecond{0.3350} & 0.2979 & 0.3155 & 0.3184 & 0.3300 & 0.3300 & 0.2906 & 0.3182 & \tblbest{0.3430} & +2.4\% \\
    \midrule
    \multirow{9}{*}{LastFM}
      & HR@5    & 0.2829 & \tblbest{0.3649} & 0.3479 & 0.3526 & 0.3573 & 0.3294 & 0.3168 & 0.3406 & 0.3430 & \tblsecond{0.3584} & -1.8\% \\
      & HR@10   & 0.3090 & \tblsecond{0.3882} & 0.3794 & 0.3831 & 0.3845 & 0.3467 & 0.3355 & 0.3586 & 0.3657 & \tblbest{0.3893} & +0.3\% \\
      & HR@20   & 0.3341 & \tblsecond{0.4081} & 0.4031 & \tblbest{0.4089} & 0.4060 & 0.3603 & 0.3525 & 0.3766 & 0.3909 & 0.4060 & -0.7\% \\
      & NDCG@5  & 0.2589 & \tblsecond{0.3184} & 0.3110 & 0.2984 & 0.3038 & 0.2998 & 0.2951 & 0.3137 & 0.3097 & \tblbest{0.3196} & +0.4\% \\
      & NDCG@10 & 0.2673 & \tblsecond{0.3260} & 0.3211 & 0.3083 & 0.3127 & 0.3053 & 0.3012 & 0.3195 & 0.3171 & \tblbest{0.3297} & +1.1\% \\
      & NDCG@20 & 0.2736 & \tblsecond{0.3311} & 0.3271 & 0.3149 & 0.3182 & 0.3088 & 0.3055 & 0.3240 & 0.3234 & \tblbest{0.3340} & +0.9\% \\
      & MRR@5   & 0.2509 & 0.3029 & 0.2987 & 0.2803 & 0.2859 & 0.2898 & 0.2877 & \tblsecond{0.3047} & 0.2986 & \tblbest{0.3067} & +0.7\% \\
      & MRR@10  & 0.2543 & 0.3060 & 0.3028 & 0.2844 & 0.2896 & 0.2921 & 0.2902 & \tblsecond{0.3071} & 0.3016 & \tblbest{0.3109} & +1.2\% \\
      & MRR@20  & 0.2561 & 0.3075 & 0.3045 & 0.2863 & 0.2911 & 0.2930 & 0.2914 & \tblsecond{0.3083} & 0.3033 & \tblbest{0.3121} & +1.2\% \\
    \midrule
    \multirow{9}{*}{ML-1M}
      & HR@5    & 0.0920 & 0.0957 & 0.1018 & 0.0915 & 0.0860 & \tblbest{0.1050} & 0.0976 & 0.0888 & \tblsecond{0.1046} & 0.0953 & -9.2\% \\
      & HR@10   & 0.1408 & 0.1478 & 0.1538 & 0.1399 & 0.1422 & \tblsecond{0.1589} & 0.1557 & 0.1501 & \tblbest{0.1594} & 0.1515 & -5.0\% \\
      & HR@20   & 0.2091 & 0.2254 & \tblsecond{0.2375} & 0.2147 & 0.2226 & 0.2282 & 0.2212 & 0.2300 & \tblbest{0.2430} & 0.2249 & -7.4\% \\
      & NDCG@5  & 0.0590 & 0.0605 & \tblsecond{0.0677} & 0.0573 & 0.0552 & \tblbest{0.0680} & 0.0667 & 0.0566 & 0.0656 & 0.0588 & -13.5\% \\
      & NDCG@10 & 0.0745 & 0.0770 & 0.0844 & 0.0728 & 0.0731 & \tblbest{0.0854} & \tblsecond{0.0853} & 0.0764 & 0.0835 & 0.0769 & -10.0\% \\
      & NDCG@20 & 0.0916 & 0.0965 & \tblbest{0.1054} & 0.0916 & 0.0932 & 0.1028 & 0.1018 & 0.0963 & \tblsecond{0.1043} & 0.0954 & -9.5\% \\
      & MRR@5   & 0.0482 & 0.0489 & \tblsecond{0.0565} & 0.0461 & 0.0451 & 0.0559 & \tblbest{0.0566} & 0.0460 & 0.0529 & 0.0469 & -17.1\% \\
      & MRR@10  & 0.0545 & 0.0556 & \tblsecond{0.0633} & 0.0525 & 0.0523 & 0.0631 & \tblbest{0.0642} & 0.0542 & 0.0603 & 0.0544 & -15.3\% \\
      & MRR@20  & 0.0591 & 0.0608 & \tblbest{0.0690} & 0.0576 & 0.0577 & 0.0678 & \tblsecond{0.0687} & 0.0595 & 0.0658 & 0.0594 & -13.9\% \\
    \midrule
    \multirow{9}{*}{Retail Rocket}
      & HR@5    & 0.4475 & \tblsecond{0.4842} & 0.4700 & 0.4611 & 0.4647 & 0.4449 & 0.4371 & 0.4305 & 0.4344 & \tblbest{0.4883} & +0.8\% \\
      & HR@10   & 0.5211 & \tblsecond{0.5605} & 0.5435 & 0.5377 & 0.5441 & 0.4658 & 0.4507 & 0.4365 & 0.5133 & \tblbest{0.5612} & +0.1\% \\
      & HR@20   & 0.5863 & \tblbest{0.6325} & 0.6135 & 0.6134 & 0.6200 & 0.4847 & 0.4629 & 0.4409 & 0.5886 & \tblsecond{0.6313} & -0.2\% \\
      & NDCG@5  & 0.3620 & 0.3867 & 0.3724 & 0.3616 & 0.3678 & \tblbest{0.3926} & 0.3892 & 0.3881 & 0.3396 & \tblsecond{0.3914} & -0.3\% \\
      & NDCG@10 & 0.3858 & \tblsecond{0.4116} & 0.3963 & 0.3864 & 0.3935 & 0.3994 & 0.3935 & 0.3900 & 0.3651 & \tblbest{0.4150} & +0.8\% \\
      & NDCG@20 & 0.4023 & \tblsecond{0.4298} & 0.4139 & 0.4057 & 0.4127 & 0.4042 & 0.3967 & 0.3911 & 0.3842 & \tblbest{0.4327} & +0.7\% \\
      & MRR@5   & 0.3335 & 0.3542 & 0.3399 & 0.3284 & 0.3355 & \tblbest{0.3748} & 0.3726 & \tblsecond{0.3733} & 0.3082 & 0.3591 & -4.2\% \\
      & MRR@10  & 0.3434 & 0.3645 & 0.3498 & 0.3388 & 0.3462 & \tblbest{0.3776} & \tblsecond{0.3745} & 0.3741 & 0.3187 & 0.3688 & -2.3\% \\
      & MRR@20  & 0.3479 & 0.3696 & 0.3547 & 0.3441 & 0.3515 & \tblbest{0.3789} & \tblsecond{0.3753} & 0.3744 & 0.3239 & 0.3737 & -1.4\% \\
    \bottomrule
  \end{tabular}}
  \caption{Full seen-target test ranking results across all cutoffs.}
  \label{tab:appendix-full-overall}
\end{table*}

The appendix table makes it easier to confirm whether a gain survives beyond the single-cutoff summary used in the main paper. In the final manuscript, this table can also absorb any dataset-specific notes about variance or significance without interrupting the narrative of the main experiments section.

\section{Extended Stage and Layout Ablations}
\label{app:stage-layout}
Figure~\ref{fig:appendix-stage-layout} expands the structural study in Figure~\ref{fig:stage-structure-panels} with the broader sweep of stage-removal and wrapper variants.

\begin{figure}[t]
  \centering
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \placeholderpanel{1.05in}

    \vspace{0.35em}
    {\footnotesize (a) Macro/mid/micro removal}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \placeholderpanel{1.05in}

    \vspace{0.35em}
    {\footnotesize (b) Dense FFN vs. 1/2/3-stage layouts}

  \end{minipage}

  \vspace{0.75em}

  \begin{minipage}[t]{\linewidth}
    \centering
    \placeholderpanel{1.15in}

    \vspace{0.35em}
    {\footnotesize (c) Wrapper-map and stage-order variants}
  \end{minipage}
  \caption{Extended stage and layout ablations. (a) Stage removal. (b) Dense replacement versus staged routing. (c) Wrapper-map and stage-order variants.}
  \Description{A three-panel placeholder figure for architecture ablations covering stage removal, dense-vs-stage-count comparisons, and wrapper or stage-order variants.}
  \label{fig:appendix-stage-layout}
\end{figure}

The figure should be read from left to right. Panel (a) verifies that macro, mid, and micro routing each contribute a distinct part of the gain. Panel (b) separates staged routing from generic expert insertion, and panel (c) checks that the final wrapper map is not an arbitrary configuration choice.

\section{Objective and Regularization Variants}
\label{app:objective}
Table~\ref{tab:appendix-objective} isolates the contribution of consistency, z-loss, and balance regularization using ranking quality, route consistency, and stability indicators.

\begin{table}[t]
  \centering
  \caption{Objective and regularization variants.}
  \label{tab:appendix-objective}
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabularx}{\columnwidth}{@{}>{\raggedright\arraybackslash}XYYY@{}}
    \toprule
    Variant & MRR@20 & Consistency & Stability \\
    \midrule
    No auxiliary loss & -- & -- & -- \\
    KNN + consistency & -- & -- & -- \\
    z-loss only & -- & -- & -- \\
    Balance loss only & -- & -- & -- \\
    Consistency + z-loss & -- & -- & -- \\
    Full objective & -- & -- & -- \\
    \bottomrule
  \end{tabularx}
\end{table}

This compact table shows whether the full objective improves MRR@20 while preserving routing consistency and stability.

\section{Extended Diagnostics}
\label{app:diagnostics}
Figure~\ref{fig:appendix-diagnostics} groups the main routing diagnostics into one coordinated layout so that stage usage, sharpness, and semantic consistency can be read together.

\begin{figure}[t]
  \centering
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \placeholderpanel{1.18in}

    \vspace{0.35em}
    {\footnotesize (a) Expert-usage heatmaps by stage}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \placeholderpanel{1.18in}

    \vspace{0.35em}
    {\footnotesize (b) Entropy and effective-expert-count summary}
  \end{minipage}

  \vspace{0.8em}

  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \placeholderpanel{1.18in}

    \vspace{0.35em}
    {\footnotesize (c) Stage-wise route consistency}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \placeholderpanel{1.18in}

    \vspace{0.35em}
    {\footnotesize (d) Feature-bucket route patterns}
  \end{minipage}
  \caption{Routing diagnostics. (a) Expert usage by stage. (b) Entropy and effective expert count. (c) Stage-wise route consistency. (d) Feature-bucket routing patterns.}
  \Description{A four-panel placeholder figure for expert-usage heatmaps, entropy summaries, stage-wise consistency, and feature-bucket route patterns.}
  \label{fig:appendix-diagnostics}
\end{figure}

These diagnostics are organized from coarse structure to semantic interpretation. The first two panels show how broad or sharp routing is at each stage, while the last two panels connect those patterns back to feature similarity and interpretable behavioral regimes.

\section{Behavior-Slice Analysis}
\label{app:behavior-slices}
Because RouteRec is motivated by heterogeneous behavior, a slice-wise view is a natural complement to the aggregate metrics. This appendix section is intended to localize gains to behavior regimes such as repeat-heavy prefixes, high-tempo sessions, narrow-focus sessions, and exploration-heavy prefixes.

\begin{figure}[t]
  \centering
  \begin{minipage}[t]{\linewidth}
    \centering
    \placeholderpanel{1.25in}

    \vspace{0.35em}
    {\footnotesize (a) Slice-wise MRR@20 across behavioral regimes}
  \end{minipage}\hfill

  \vspace{0.75em}

  \begin{minipage}[t]{\linewidth}
    \centering
    \placeholderpanel{1.25in}

    \vspace{0.35em}
    {\footnotesize (b) Relative RouteRec gain and route concentration by slice}
  \end{minipage}
  \caption{Behavior-slice analysis. (a) Slice-wise ranking quality. (b) Relative gain and routing concentration by slice.}
  \Description{A two-panel placeholder figure for slice-wise MRR@20 and RouteRec gain across repeat-heavy, fast-tempo, focused, and exploration-heavy regimes.}
  \label{fig:appendix-behavior-slices}
\end{figure}

The useful reading order is to first check which slices benefit most in panel (a), then verify in panel (b) whether those gains coincide with more decisive and semantically aligned routing. This experiment fits the paper's main argument especially well because it answers not only whether RouteRec helps, but also where the help appears.

\section{Extended Transfer Results}
\label{app:transfer}
This section reports additional transfer experiments for low-resource and cross-domain adaptation.

\begin{table}[t]
  \centering
  \caption{Source-target transfer matrix.}
  \label{tab:appendix-transfer-matrix}
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}}lcccccc@{}}
    \toprule
    Src. $\rightarrow$ Tgt. & Bty & Fsq & KRec & LFM & ML1M & RR \\
    \midrule
    Bty & -- & -- & -- & -- & -- & -- \\
    Fsq & -- & -- & -- & -- & -- & -- \\
    KRec & -- & -- & -- & -- & -- & -- \\
    LFM & -- & -- & -- & -- & -- & -- \\
    ML1M & -- & -- & -- & -- & -- & -- \\
    RR & -- & -- & -- & -- & -- & -- \\
    \bottomrule
  \end{tabular*}
\end{table}

The matrix provides the exact source-target view that is too detailed for the main paper. It is useful for identifying asymmetric transfer pairs and for checking whether router reuse is most effective between domains with similar temporal and repetition structure. For compactness, the table uses the abbreviations Bty, Fsq, KRec, LFM, ML1M, and RR.

\begin{figure}[t]
  \centering
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \placeholderpanel{1.05in}

    \vspace{0.35em}
    {\footnotesize (a) Low-resource transfer curves}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \placeholderpanel{1.05in}

    \vspace{0.35em}
    {\footnotesize (b) Frozen/finetuned/anchor and group/full router transfer}

  \end{minipage}

  \vspace{0.75em}

  \begin{minipage}[t]{\linewidth}
    \centering
    \placeholderpanel{1.15in}

    \vspace{0.35em}
    {\footnotesize (c) Multi-source sequential order effect}
  \end{minipage}
  \caption{Transfer variants. (a) Low-resource transfer curves. (b) Frozen, finetuned, anchor, and router-granularity comparisons. (c) Multi-source order effects.}
  \Description{A three-panel placeholder figure for low-resource transfer curves, transfer-strategy comparisons, and multi-source order effects.}
  \label{fig:appendix-transfer-panels}
\end{figure}

Together, the table and figure let the reader move from exact pairwise results to higher-level patterns. The intended appendix message is that transfer is asymmetric, most helpful in related domains and low-resource regimes, and partially preserved even under coarser router reuse.

\end{document}
