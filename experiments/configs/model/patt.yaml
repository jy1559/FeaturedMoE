# @package _global_
# PAtt - Pattern-Aware Sequential Recommendation

model: PAtt

num_layers: 2
num_heads: 2
hidden_size: 128
embedding_size: 128
inner_size: 256
hidden_dropout_prob: 0.1

diversity_gamma: 0.1
loss_type: CE

search:
  num_layers: [1, 2, 3]
  diversity_gamma: [0.05, 0.1, 0.2]
