# @package _global_
# MSSR - Multi-granularity Sequential Recommendation

model: MSSR

selected_features: [category]
attribute_hidden_size: 128

num_layers: 2
num_heads: 2
hidden_size: 128
embedding_size: 128
inner_size: 256
hidden_dropout_prob: 0.1
loss_type: CE

search:
  num_layers: [1, 2, 3]
  attribute_hidden_size: [32, 64, 128]
