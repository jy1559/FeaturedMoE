# @package _global_
# SASRec - Self-Attentive Sequential Recommendation

model: SASRec

hidden_size: 128
embedding_size: 128
num_layers: 2
num_heads: 4
inner_size: 256

search:
  attn_dropout_prob: [0.0, 0.1, 0.2, 0.3]
  num_layers: [1, 2, 3]
