# @package _global_

# SESSION_FIXED mode:
# Use pre-generated session-level split files:
#   {dataset}.train.inter / {dataset}.valid.inter / {dataset}.test.inter
# where sessions are split by session start time (temporal, no overlap).

benchmark_filename: [train, valid, test]

eval_args:
  group_by: user
  order: TO
  mode: full
