# @package _global_

# Basic features: Use basic datasets with only core columns (faster loading)
# Columns: session_id, item_id, timestamp, user_id (+ category in .item)
# Best for: SASRec, GRU4Rec, and other baseline models

data_path: ${dataset_root}/basic
